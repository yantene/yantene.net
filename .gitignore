.DS_Store
.env
/tmp
/node_modules/
.pnpm-store
*.tsbuildinfo

# Vite
/dist/

# Cloudflare
.mf
.wrangler
.dev.vars*
worker-configuration.d.ts

# Storybook
storybook-static
debug-storybook.log

# Claude Code
.claude/settings.local.json
.claude/worktrees/

# Generated by @hono/inertia
/app/frontend/pages.gen.ts

!.dev.vars.example
!.env.example

# React Router (generated types)
/.react-router/

# React Router build output
/build/

# postinstall が node_modules から作り直す (scripts/copy-emoji-svg.mjs)
public/emoji/svg/

# コンテンツリポジトリ (Cloudflare Artifacts) の作業用 clone。
# ここで記事を書き、artifacts-production / artifacts-staging へ push する。
# 別リポジトリなので、このリポジトリの履歴にも lint / 整形の対象にも入れない。
/content/
