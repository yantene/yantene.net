/**
 * 人気の重みが半分になるまでの日数。
 *
 * 短いほど直近の勢いを拾い、長いほど落ち着いた人気を映す。記事もアクセスも多くない
 * うちは短くすると数件の差で順位が跳ねるため、やや長めに取る。
 *
 * 記録する側と並べる側で必ず同じ値を使う。食い違うと、書いたときの重みと読むときの
 * 重みが噛み合わず、スコアの意味が壊れる。
 */
export const VIEW_SCORE_HALF_LIFE_DAYS = 30;
